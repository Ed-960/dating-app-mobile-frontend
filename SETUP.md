# Настройка приложения перед запуском

## 📋 Предварительные требования

1. **Node.js** 18+ установлен
2. **npm** или **yarn** установлен
3. **Expo CLI** (устанавливается автоматически с зависимостями)

## 🎨 Создание иконок и изображений

Приложение требует следующие файлы в папке `assets/`:

### Обязательные файлы:

1. **icon.png** (1024x1024px)
   - Главная иконка приложения
   - Формат: PNG с прозрачностью
   - Размер: 1024x1024 пикселей

2. **splash.png** (1284x2778px для iOS, 1080x1920px для Android)
   - Экран загрузки
   - Рекомендуется: логотип по центру на белом фоне

3. **adaptive-icon.png** (1024x1024px) - для Android
   - Адаптивная иконка
   - Безопасная зона: 1024x1024, но важные элементы в центре 768x768

4. **favicon.png** (48x48px) - для Web
   - Иконка для браузера

### Быстрое создание:

Можно использовать онлайн генераторы:
- [App Icon Generator](https://www.appicon.co/)
- [Expo Icon Generator](https://www.favicon-generator.org/)

Или создать вручную в графическом редакторе.

### Структура папки assets:

```
frontend/
  assets/
    icon.png
    splash.png
    adaptive-icon.png
    favicon.png
```

## ⚙️ Настройка для разработки

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Настройка Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID:
   - Тип: **Web application**
   - Authorized redirect URIs: `datingapp://oauth2redirect`
5. Скопируйте Client ID

### 3. Обновление конфигурации

Отредактируйте `app.json` или используйте переменные окружения:

```bash
# В app.json измените:
"GOOGLE_CLIENT_ID": "ваш_client_id.apps.googleusercontent.com"
```

Или через переменные окружения:
```bash
export GOOGLE_CLIENT_ID="ваш_client_id.apps.googleusercontent.com"
```

### 4. Запуск бэкенда

Убедитесь, что бэкенд запущен на `http://localhost:3000`:

```bash
cd ../backend
npm install
npm run start:dev
```

## 🚀 Запуск приложения

### iOS Simulator (только на Mac)

```bash
npm run ios
```

### Android Emulator

1. Установите Android Studio
2. Создайте эмулятор
3. Запустите:
```bash
npm run android
```

### Физическое устройство

1. Установите Expo Go на устройство:
   - [iOS App Store](https://apps.apple.com/app/expo-go/id982107779)
   - [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)

2. Запустите:
```bash
npm start
```

3. Отсканируйте QR код в Expo Go

### Web

```bash
npm run web
```

## 🔧 Настройка для разных окружений

### Development (локально)

Используется автоматически при `npm start`:
- Backend URL: `http://localhost:3000`
- Название приложения: "Dating App (Dev)"

### Production

Настройте через EAS секреты перед сборкой:

```bash
eas secret:create --scope project --name BACKEND_URL --value https://your-api.com
eas secret:create --scope project --name GOOGLE_CLIENT_ID --value your_production_client_id
```

## 📱 Настройка Bundle ID / Package Name

Перед публикацией в магазины приложений измените:

### iOS (app.json):
```json
"ios": {
  "bundleIdentifier": "com.yourcompany.datingapp"
}
```

### Android (app.json):
```json
"android": {
  "package": "com.yourcompany.datingapp"
}
```

**Важно:** Используйте уникальные идентификаторы для вашего приложения!

## ✅ Проверка перед деплоем

- [ ] Все иконки созданы и размещены в `assets/`
- [ ] Google OAuth Client ID настроен
- [ ] Bundle ID / Package Name изменены на уникальные
- [ ] Бэкенд развернут и доступен
- [ ] Переменные окружения настроены для production
- [ ] Приложение протестировано на реальных устройствах

## 🆘 Проблемы и решения

### Ошибка: "Cannot find module './assets/icon.png'"

**Решение:** Создайте папку `assets/` и добавьте необходимые файлы (см. выше)

### Ошибка: "Google OAuth failed"

**Решение:**
1. Проверьте Client ID в `app.json`
2. Убедитесь, что redirect URI правильный: `datingapp://oauth2redirect`
3. Проверьте, что Web Client ID используется (не iOS/Android)

### Ошибка: "Network request failed"

**Решение:**
1. Убедитесь, что бэкенд запущен
2. Проверьте `BACKEND_URL` в конфигурации
3. Для физических устройств используйте IP адрес вместо localhost:
   ```json
   "BACKEND_URL": "http://192.168.1.100:3000"
   ```

### Ошибка при сборке: "Missing icon"

**Решение:** Убедитесь, что все файлы в `assets/` существуют и имеют правильные размеры
