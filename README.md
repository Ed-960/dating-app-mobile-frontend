# Frontend - Dating App

React Native приложение на Expo для знакомств.

## 🚀 Быстрый старт

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные окружения:
```bash
# Для разработки создайте .env файл (опционально)
# BACKEND_URL будет автоматически использоваться из app.json или app.config.js
```

3. Запустите приложение:
```bash
# Разработка (localhost)
npm start

# Или с явным указанием URL
npm run start:dev
```

## 📱 Разработка на устройствах

### iOS
```bash
npm run ios
# Или для разработки с localhost
npm run ios:dev
```

### Android
```bash
npm run android
# Или для разработки с localhost
npm run android:dev
```

## 🌍 Разные окружения

Приложение автоматически определяет окружение через `app.config.js`:

- **Development**: Использует `http://localhost:3000`
- **Production**: Использует URL из переменных окружения или EAS секретов

Для настройки production URL используйте EAS:
```bash
eas secret:create --scope project --name BACKEND_URL --value https://your-api.com
```

## 📱 Платформы

- **iOS**: `npm run ios`
- **Android**: `npm run android`
- **Web**: `npm run web`

## 🏗️ Архитектура

### Структура

- `api/` - API клиент и функции для работы с бэкендом
- `contexts/` - React контексты (AuthContext)
- `hooks/` - Custom React hooks (useApi)
- `screens/` - Экраны приложения (Login, Home)
- `constants/` - Константы и конфигурация

### Технологии

- **React Native** (Expo 52)
- **React Navigation** - Навигация
- **TanStack Query** - Управление состоянием и кэширование
- **Expo Auth Session** - OAuth аутентификация
- **Expo Secure Store** - Безопасное хранение токенов
- **TypeScript** - Типизация

## 🔐 Аутентификация

Приложение использует Google OAuth 2.0 с PKCE:

1. Пользователь нажимает "Sign in with Google"
2. Открывается браузер с Google OAuth
3. После авторизации, код обменивается на JWT токены
4. Токены сохраняются в Secure Store
5. Access token автоматически обновляется при истечении

## 📝 Основные компоненты

### AuthContext

Управляет состоянием аутентификации:
- `user` - Текущий пользователь
- `tokens` - JWT токены
- `signIn()` - Вход через Google
- `signOut()` - Выход
- `isLoading` - Состояние загрузки

### useApi Hook

Хук для выполнения API запросов с автоматическим обновлением токенов:

```typescript
const { data, isLoading } = useApi<User>('/auth/me');
```

## 🎨 UI/UX

- Современный дизайн с градиентами
- Адаптивная верстка
- Плавные анимации
- Обработка состояний загрузки и ошибок

## 🔧 Конфигурация

Основные переменные окружения:

- `BACKEND_URL` - URL бэкенд API
- `GOOGLE_CLIENT_ID` - Google OAuth Client ID (Web Client ID)

## 📦 Зависимости

Основные пакеты:
- `@react-navigation/native` - Навигация
- `@tanstack/react-query` - Запросы и кэширование
- `expo-auth-session` - OAuth
- `expo-secure-store` - Безопасное хранилище
- `@expo/vector-icons` - Иконки

## 🐛 Troubleshooting

### Проблемы с подключением к бэкенду

Убедитесь, что:
1. Backend запущен и доступен
2. `BACKEND_URL` в `.env` правильный
3. CORS настроен на бэкенде

### Проблемы с OAuth

1. Проверьте `GOOGLE_CLIENT_ID` в `.env`
2. Убедитесь, что используется Web Client ID (не iOS/Android)
3. Проверьте redirect URI в Google Console

## 📝 Лучшие практики

- ✅ TypeScript strict mode
- ✅ Типизация всех компонентов
- ✅ Обработка ошибок
- ✅ Оптимизация производительности (React.memo, useMemo)
- ✅ Безопасное хранение токенов
- ✅ Автоматическое обновление токенов
